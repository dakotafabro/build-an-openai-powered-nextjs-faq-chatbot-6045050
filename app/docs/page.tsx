/**
 * Dakota: Notes for this chapter.
 */
export default function DocsPage() {
  return (
    <main className="panel">
      <h1>03-02 Notes — Provider + RTK Query</h1>
      <ul>
        <li>App wrapped in Redux <code>Provider</code></li>
        <li>RTK Query slice with placeholder endpoints</li>
        <li>Components call hooks; no backend required yet</li>
      </ul>
    </main>
  );
}
